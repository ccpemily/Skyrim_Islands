{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch RimWorld",
      "type": "node-terminal",
      "request": "launch",
      "command": "powershell -NoProfile -Command \"$process = Get-Process RimWorldWin64 -ErrorAction SilentlyContinue; if (-not $process) { Start-Process -FilePath '${workspaceFolder}\\..\\..\\RimWorldWin64.exe' -WorkingDirectory '${workspaceFolder}\\..\\..' }\"",
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "build mod"
    }
  ]
}
