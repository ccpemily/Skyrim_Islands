using Verse;

namespace SkyrimIslands.Quests.Initial.Shuttle
{
    public class CompProperties_SkyIslandMissionShuttleControl : CompProperties
    {
        public CompProperties_SkyIslandMissionShuttleControl()
        {
            compClass = typeof(CompSkyIslandMissionShuttleControl);
        }
    }
}
